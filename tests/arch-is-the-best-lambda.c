#include <stdio.h>
#include <stdlib.h>
#include <prelude.h>
int main(void) start(
   void (*arch_best)(void) = lambda(void, (void))(puts("Arch is the best!"); mkvoid());
   arch_best();
   EXIT_SUCCESS)

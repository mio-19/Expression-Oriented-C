#include <iostream>
#include <cstdlib>
#include <prelude.h>
int main () start(
   let arch_best = lambda_returns(void, int x)(std::cout << "Arch is the best! " << x << std::endl;);
   arch_best(9999);
   EXIT_SUCCESS)

#include <iostream>
#include <cstdlib>
#include <prelude.h>
int main () start(
   let arch_best = lambda(void, (void))(std::cout << "Arch is the best!" << std::endl;);
   arch_best();
   EXIT_SUCCESS)
